/**
 * This script handles the file-handling functionality
 */
const fs = require('fs');

const writeFile = fileContent => new Promise((resolve, reject) => {});
const copyFile = fileContent => new Promise((resolve, reject) => {});

module.exports = { writeFile, copyFile };